# Types
type User {
  id: String!
  name: String!
  email: String!
}

# Queries

type Query {
  hello(name: String!): String
}

# Mutations

type Mutation {
  register(name: String!, email: String!, password: String!): User
}
